import { useAppDispatch, useAppSelector } from '../../app/hook'
import { decrement, increment } from '../counter/counterSlice'

const TimeList = () => {
  return <div>Khung giờ</div>
}

export default TimeList
